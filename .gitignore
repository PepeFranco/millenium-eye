# Virtual environment
.venv/

# Downloaded card images (large, re-downloadable)
data/images/
data/images_full/

# Synthetic training data (large, regeneratable)
data/synthetic/

# Training artefacts (large, not needed on server)
data/finetuned_mobilenet.pth

# ORB descriptor index (large, regeneratable)
data/orb_descriptors.npy
data/orb_card_ids.npy

# Old phash artefacts (replaced by ORB)
data/hashes.db
data/hash_index.pkl
data/phash_hashes.npy
data/phash_card_ids.npy
data/phash_card_names.json

# Live database (not for version control)
data/wishlist.db

# Reference data (re-downloadable)
data/edison_cards.json
data/sets.json

# Logs
data/failed_downloads.txt

# Debug artefacts
data/debug_*.jpg
test_card.png

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
